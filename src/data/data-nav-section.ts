// ─── PlatformNavSection — /platform/data ──────────────────────────────────────
//
// This value controls which card in the platform navigation is treated as the
// "current" (active / dimmed) page.  Change it to match whichever /platform/*
// route this file belongs to if you ever move or rename the Data page.

export const DATA_NAV_CURRENT_HREF = '/platform/data'
